import React from 'react';
import FooterForm from './Footer-form';

interface IFooter {

}

const Footer: React.SFC<IFooter> = ({

}) => {
    return (
        <FooterForm/>
    );
};

export default Footer;
