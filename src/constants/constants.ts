const constants: {
    SMTP_HOST: string,
    SMTP_PORT: number,
    SMTP_SECURE: boolean,
} = {
    SMTP_HOST: 'smtp.gmail.com',
    SMTP_PORT: 465,
    SMTP_SECURE: true,
};

export default constants;
